# ByteHunter Backend Skeleton

当前目录用于承载 ByteHunter 的 FastAPI 后端。

首阶段已预留：

- `src/main.py` 作为应用入口
- `src/config.py` 作为环境配置层
- `src/api/` 作为路由层
- `src/core/` 作为数据库与基础设施层
- `src/schemas/` 作为 Pydantic 模型层

后续任务会在这里接入 PostgreSQL、Redis、Judge0 与 AI 服务。
